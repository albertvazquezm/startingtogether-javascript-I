/**
 * Operadores lógicos
 */

/**
 * Una condición
 * - Operadores de comparación (== === > < >= <=)
 * - Conversión de tipos
 */

if (true) {
    console.log(`hola!`);
}

/**
 * Múltiples condiciones
 * - ||
 * - &&
 */
