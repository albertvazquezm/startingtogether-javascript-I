/**
 * Para qué se usan las variables
 */


/**
 * Cómo se definen (var, let y const)
 * - caracteres prohibidos
 * - buenas prácticas (descriptivas, camelCase, CONSTANT)
 * - reasignar vs mutar
 */

const url = {base: `a.com`};
url.base = `b.com`;

console.log(url);

/**
 * var vs let y const
 * - peculiaridades var: hoisting, scope
 */
